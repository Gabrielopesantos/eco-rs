Rust implementation of [eco](https://github.com/gabrielopesantos/eco) to get familiarized with the language.


Still missing:
    - Having multiple concurrent HTTP "servers" waiting for connections;

